// Creating and array and using for loop to iterate over it.
let li = ['lion', 12, 'tiger', 6, 'elephant', 50, 'whales', 70];
for (let i = 0; i <= li.length; i++) {
    console.log(li[i])
}

//using other propety while using for loop on array.
li = ['lion', 'tiger', 'elephant', 'whale'];
for (i = 0; i <= li.length; i++) {
    console.log(li[i].toUpperCase())
}